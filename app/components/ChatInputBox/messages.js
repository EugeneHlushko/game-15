/*
 * ChatInputBox Messages
 *
 * This contains all the text for the ChatInputBox component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  send: {
    id: 'app.components.ChatInputBox.send',
    defaultMessage: 'Send',
  },
});
